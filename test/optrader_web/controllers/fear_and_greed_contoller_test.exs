defmodule OptraderWeb.FearAndGreedControllerTest do
  use OptraderWeb.ConnCase

  # test "#index renders a list of todos" do
  #   conn = build_conn()
  #   fear_and_greed = insert(:fear_and_greed)

  #   conn = get conn, fear_and_greed_path(conn, :index)

  #   assert json_response(conn, 200) == %{
  #     "data" => [%{
  #       "title" => todo.title,
  #       "description" => todo.description,
  #       "inserted_at" => Ecto.DateTime.to_iso8601(todo.inserted_at),
  #       "updated_at" => Ecto.DateTime.to_iso8601(todo.updated_at)
  #     }]
  #   }
  # end
end
